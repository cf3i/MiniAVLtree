# Progress

> 本文档回答：项目现在长什么样？
>
> 只记录事实状态，不写未来意图。未来意图归 `docs/plan/backlog.md`。

## 更新时间

- 日期：2026-03-29
- 维护人：Agent

## 项目阶段

- 当前阶段：开发中
- 当前里程碑：初始 AVL 树示例代码已导入仓库；当前可见的最近提交为 `16b5629 Add files via upload`

## 已完成功能

- [x] 已实现整数 AVL 树节点结构，节点保存 `val`、`height` 以及左右子节点指针。
- [x] 已实现 AVL 树插入逻辑，并在失衡时执行 LL、RR、LR、RL 四类旋转。
- [x] 已实现按整数值递归查找节点。
- [x] 已实现中序遍历输出。
- [x] 已实现前序遍历输出。
- [x] 已实现树析构时的递归释放。
- [x] 已实现命令行入口：程序固定读取 10 个整数，逐次插入后输出当前树的中序和前序遍历结果。

## 已知问题

- （暂无已知问题）

## 技术债

- `AVLtree::remove` 与 `AVLtree::__remove` 当前只有声明，没有定义；公开接口与实际实现不完全一致。
- `node::parent` 字段已声明，但当前插入与旋转逻辑没有维护该指针，属于未使用状态。
- 程序入口将输入协议硬编码为固定读取 10 个整数，缺少参数化和输入校验。
- `issue_test` 目录当前仅见 `README.md`，未见实际 `issue_test/*.sh` 回归脚本。
- CI / 自动化质量门当前未配置；仓库根目录当前未见 `.github` 等持续集成配置。
